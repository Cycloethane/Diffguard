# 项目级安全策略即配置：DiffGuard 生成的 .opencode/ 模板
# 复制到任意项目根目录即可启用 DiffGuard 安全审查与决策协作。

{
  "mcp": {
    "diffguard": {
      "type": "stdio",
      "command": ["python", "-m", "bridge.mcp_server"],
      "cwd": "<DiffGuard 仓库路径>"
    }
  },

  "permissions": {
    "allow": [
      "diff",
      "git status",
      "git diff*",
      "git log*"
    ],
    "ask": [
      "git reset*",
      "git clean*",
      "git push --force*",
      "rm -rf*",
      "del /s*",
      "rd /s*",
      "reg delete*",
      "taskkill*"
    ]
  },

  "skills": {
    "diffguard": "<DiffGuard 仓库路径>/opencode/SKILL.md"
  }
}
