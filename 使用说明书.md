# DiffGuard 使用说明书

版本：0.0.3（便携版）　适用平台：Windows 10 / 11（64 位）

---

## 目录

1. [软件简介](#1-软件简介)
2. [安装与启动](#2-安装与启动)
3. [首次配置](#3-首次配置)
4. [界面总览](#4-界面总览)
5. [核心功能：AI 审查 Diff](#5-核心功能ai-审查-diff)
6. [核心功能：权限审批监控](#6-核心功能权限审批监控)
7. [历史记录](#7-历史记录)
8. [设置项详解](#8-设置项详解)
9. [前台模式](#9-前台模式)
10. [数据存储位置](#10-数据存储位置)
11. [快捷键一览](#11-快捷键一览)
12. [常见问题](#12-常见问题)

---

## 1. 软件简介

DiffGuard 面向需要频繁审阅代码变更的开发人员，解决两个痛点：

- **审查 diff 太累**：把 git diff 粘贴进来，AI 自动生成风险报告，并结合本地启发式给出 0-100 风险分。
- **AI 工具乱要权限**：现在的 AI 编程助手（ZCode、OpenCode、Cursor、Cline 等）会在执行命令、读写文件前弹出授权请求，用户容易随手点"允许"。DiffGuard 会识别这类请求、评估其风险，并用置顶浮窗提醒你谨慎决策。

> 提示：风险评分与权限识别均为本地启发式结果，**仅供决策参考**，不构成安全结论。

---

## 2. 安装与启动

### 方式一：便携版（推荐测试）

1. 解压 zip 到任意目录（路径建议不要含中文与空格）。
2. 双击 `DiffGuard.exe` 即可启动，无需安装、不写注册表。
3. 桌面可能生成 `@AutomationLog.txt` 等 UIA 临时日志，属正常现象，可删除。

### 方式二：安装版

1. 双击 `DiffGuardSetup-0.0.3.exe`。
2. 若 SmartScreen 提示"未知发布者"，点击 **更多信息 → 仍要运行**（安装包未签名）。
3. 按向导安装，可选创建桌面快捷方式。

> 安装版为**单用户安装**（安装到当前用户目录），无需管理员权限。

### 常见启动问题

| 现象 | 处理 |
|---|---|
| 双击无反应 | 关闭杀软/Defender 对未知发布者的拦截；或右键"以管理员身份运行" |
| 提示缺少 DLL | 说明解压不完整，重新解压整个文件夹（需保留 `_internal` 目录） |
| 弹出黑色控制台窗口 | 属 UIA 依赖初始化输出，属正常，可忽略；不影响使用 |

---

## 3. 首次配置

1. 启动后点击工具栏 **设置**。
2. 填入 **API Key (SiliconFlow)**：到 SiliconFlow 平台创建，形如 `sk-...`。
3. 选择 **模型**（默认 DeepSeek-V4-Flash，其余需平台已开通）。
4. 选择 **界面主题**（深色/浅色）与 **强调色**（蓝/绿/紫/橙）。
5. 按需开关功能（见[第 8 节](#8-设置项详解)），点击 **保存**。

> 未配置 API Key 时，AI 审查不可用，但风险评分、权限监控、历史记录等本地功能不受影响。

---

## 4. 界面总览

- **顶部工具栏**：Logo＋◆DiffGuard｜从剪贴板粘贴｜开始审查｜💾 保存｜📤 导出｜设置｜历史记录｜权限记录｜前台模式
- **顶部仪表盘**：载入 diff 后显示风险总分、等级徽章、文件数与主要风险点
- **左侧（变更文件区）**：按风险着色的文件列表（红=高 黄=中 绿=低，⚠/🔴 标记高风险项，悬停看详情），下方为单个文件的 Diff 详情
- **右侧（AI 审查报告区）**：风险进度条（0-100，蓝→绿→黄→橙→红）＋ 流式报告
- **底部状态栏**：操作状态与剪贴板/权限监控在线指示

> 启动后未载入 diff 时，主体区域显示**使用引导**，粘贴或载入 diff 后自动消失。

---

## 5. 核心功能：AI 审查 Diff

### 5.1 载入 diff

任选其一：

- 复制 `git diff` 输出 → 程序**自动检测**并载入（需开启"自动监听剪贴板"）。
- 点击 **从剪贴板粘贴**。
- 手动粘贴到 Diff 详情框后点击 **开始审查**。

> diff 会被截断/不完整时，状态栏会提示"内容不完整，仅粗略解析"。

### 5.2 开始审查

1. 点击 **开始审查**，后台线程调用模型流式生成报告。
2. 报告逐字显示在右侧，进度条实时反映本地风险评分。
3. 完成后状态栏提示"审查完成，可保存到历史"。

### 5.3 风险评分明细

本地评分由以下维度累加（上限 100）：

- 风险标记：疑似硬编码密钥(+30)、配置文件变更(+20)、文件删除(+20)、依赖变更(+15)
- 变更规模：行数越多加分越多，但对数递减，防止大 diff 刷分
- 风险文件数量、删除占比（≥50% 视为重写倾向）

### 5.4 保存历史

点击 **保存到历史**，将当前审查结果存入本地数据库，供后续回看。

---

## 6. 核心功能：权限审批监控

### 6.1 原理

- **主通道（UI Automation）**：每 2 秒扫描一次顶层窗口，收集控件文本，识别 AI 工具的授权请求。
- **辅助通道（剪贴板）**：当你复制了权限请求文本时同步识别。
- 识别到后：`证据判定 → 解析目标/动作 → 本地风险评分 → 存入权限历史 → 弹出置顶浮窗`。

### 6.2 权限审批浮窗

浮窗会显示：

- 来源（ZCode / OpenCode / Cursor / Cline / Clipboard / Unknown）
- 类型（文件访问 / 命令执行 / 网络拉取 / 配置变更）
- 动作（读 / 写 / 执行 / 删除 / 拉取）
- 目标路径 / URL / 命令，及展开后的绝对路径
- 风险分数与评分明细、检测到的按钮选项

三个决策按钮：

| 按钮 | 含义 | 后续动作 |
|---|---|---|
| 允许一次 | 仅本次允许 | 尝试在原窗口点击 "Allow once" |
| 总是允许 | 永久允许 | 尝试在原窗口点击 "Allow always" |
| 拒绝 | 拒绝请求 | 尝试在原窗口点击 "Reject/Deny" |

> **回写说明**：对带原生授权按钮的 GUI 工具，会尽力通过 UIA 点击原窗口按钮；对终端类 TUI 工具（如 opencode），默认仅记录决策到历史。若在设置中开启"键盘注入回写"，会对 TUI 工具尝试发送方向键＋回车模拟选择（实验性，默认关闭）。

### 6.3 自动放行（低风险静默通过）

开启设置中的"低风险权限请求自动放行"后：

- 当某权限请求的**风险分低于阈值**（默认 20，可在 5-40 间滑杆调节）时，程序**不弹浮窗**，自动按"允许一次"处理并写入权限历史。
- 适合审查宽松的环境，减少打扰；介意时可直接关闭或在权限记录中查看被自动放行的条目。

### 6.4 高风险托盘通知

开启"高风险权限请求发送系统托盘通知"后，**风险分 ≥ 60** 的权限请求会在浮窗弹出的同时，于系统托盘弹出气球通知，避免切走窗口时错过高风险请求。

### 6.5 权限记录

点击工具栏 **权限记录**，可查看所有权限审批记录（时间、来源、类型、动作、风险、目标、决策状态）。

---

## 7. 历史记录

- **历史记录**：AI 审查记录。支持顶部**搜索框**按关键字过滤，以及**风险等级下拉**（全部/低/中/高）筛选。点击 **详情** 可查看完整报告与原始 diff，并可更改决策（批准/拒绝）。
- **权限记录**：权限审批记录（见 [6.5](#65-权限记录)）。

两者分别存储于同一 SQLite 数据库的不同表，均按时间倒序排列。

## 7.1 导出报告

点击工具栏 **📤 导出报告**（或 Ctrl+E），可将当前 AI 审查报告与风险评分导出为文件，可选 **Markdown / HTML / 纯文本** 三种格式（默认 Markdown），导出路径通过文件对话框选择。

---

## 8. 设置项详解

| 设置项 | 默认 | 说明 |
|---|---|---|
| API Key | 空 | SiliconFlow 平台密钥，AI 审查必需 |
| 模型 | DeepSeek-V4-Flash | 审查使用的模型 |
| 界面主题 | 深色 | dark / light |
| 强调色 | 蓝 | blue / green / purple / orange |
| 自动监听剪贴板 | 开 | 复制 git diff 自动载入；同时作为权限辅助通道 |
| 权限审批监控 | 开 | 开启 UIA 主通道扫描窗口识别授权请求 |
| 权限审批浮窗置顶 | 开 | 权限浮窗是否以置顶方式显示（关闭则仅在状态栏提示+记录） |
| 高风险托盘通知 | 开 | 风险分 ≥60 的权限请求发送系统托盘气球通知 |
| 低风险自动放行 | 关 | 风险分低于阈值的请求不弹浮窗、自动放行并记录 |
| 自动放行阈值 | 20 | 风险分阈值（5-40，滑杆调节）；"风险分 < 阈值"时放行 |
| 键盘注入回写 | 关 | 对 TUI 终端工具用方向键+回车模拟决策（实验性） |
| 启动时检查更新 | 开 | 启动后后台检查 GitHub 发布页是否有新版本 |

---

## 9. 前台模式

点击 **前台模式** 打开迷你悬浮窗，常驻置顶显示当前 diff 的状态、文件数与风险分，方便审查时切到其它窗口对照。再次点击可隐藏。

---

## 10. 数据存储位置

| 内容 | 路径 |
|---|---|
| 配置文件 | `%APPDATA%\DiffGuard\config.json` |
| 数据库 | `%LOCALAPPDATA%\DiffGuard\diffguard.db` |
| 日志 | `%LOCALAPPDATA%\DiffGuard\Logs\` |

> 删除安装目录**不会**删除这些数据；卸载/清理请手动删除上述目录。

---

## 11. 快捷键一览

| 快捷键 | 功能 |
|---|---|
| Ctrl+V | 从剪贴板载入 git diff |
| Ctrl+R / Ctrl+Enter | 开始 AI 审查 |
| Ctrl+S | 保存当前审查到历史 |
| Ctrl+E | 导出报告 |

---

## 12. 常见问题

**Q1：为什么偶尔自动弹权限浮窗，内容又像软件自身？**
不会。UIA 扫描已排除本进程窗口，且剪贴板通道只在**新内容变化**时触发。若浮窗内容描述的是某工具的操作，说明该工具正请求权限。

**Q2：AI 报告一直"分析中"？**
请确认：API Key 是否正确、模型是否开通、网络能否访问 SiliconFlow。可查看日志目录下的日志文件。

**Q3：权限浮窗的按钮点了没反应？**
对终端类 TUI 工具（如 opencode），默认回写按钮不适用，决策仅记录。可开启"键盘注入回写"（实验性）或在工具界面内手动确认。

**Q4：有些权限请求直接放行了，没经过我确认？**
说明开启了"低风险自动放行"且该请求风险分低于阈值（默认 20）。可在设置中关闭，或调低阈值。

**Q5：系统托盘没看到高风险通知？**
请确认设置中"高风险托盘通知"已开启（默认开）。部分系统/第三方软件会拦截托盘气球，此时可依赖置顶浮窗。

**Q6：启动时提示"发现新版本"？**
程序启动后会在后台查询 GitHub 发布页。若提示新版，可前往发布页下载更新；网络不可用时检查静默跳过，不影响使用。

**Q7：如何卸载安装版？**
控制面板 → 卸载程序 → DiffGuard，或运行安装目录下的卸载程序。

**Q8：便携版和安装版选哪个？**
测试/分发选便携版（免安装）；正式使用选安装版（含开始菜单与卸载）。

---

© 2026 DiffGuard Team　本说明书随软件分发，如有更新以程序内界面为准。
